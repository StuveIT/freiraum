<main>
	<slot />
</main>

<style>
    .card {
		border-radius: var(--border-radius);
		padding: 2em;
		margin: 1em;
		box-shadow: var(--standard-box-shadow);
		background-color: var(--background);
	}

	.card > h1,
	.card > h2,
	.card > h3,
	.card > h4,
	.card > h5,
	.card > h6 {
		margin-top: 0;
	}
</style>
